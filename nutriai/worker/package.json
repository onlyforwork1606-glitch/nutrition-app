{
  "name": "nutriai-worker",
  "version": "1.0.0",
  "private": true,
  "description": "NutriAI Cloudflare Worker API (AI proxy, D1, R2, auth, cron).",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.6.3",
    "@hono/zod-validator": "^0.4.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241121.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0",
    "vitest": "^2.1.8"
  }
}
